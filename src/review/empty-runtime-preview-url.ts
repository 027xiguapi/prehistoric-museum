// Production/e2e stub for `$museum-review-runtime`. The caller guards the
// only call site with `museumMode === 'review'`, so this is never invoked.

export function reviewModelPreviewUrl(
  animalId: string,
  fileName: string,
): string {
  void animalId
  void fileName
  return ''
}
