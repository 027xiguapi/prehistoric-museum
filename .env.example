gmi-key=

# Remote origin for on-demand model/narration assets (see scripts/generate-remote-assets.mjs).
# Leave empty for the web build (same-origin /animals/...).
# Set to an HTTPS origin for the Capacitor build, e.g. https://museum.routerpark.com
NEXT_PUBLIC_ASSET_ORIGIN=
